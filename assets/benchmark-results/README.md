# Benchmark Results

Use this folder for distilled benchmark observations after a real run has been executed.

Keep the raw run artifacts in the local workspace.

Store only the reusable summary in this repository:

- which eval ran
- what the result was
- what the result teaches about the skill
- what the result teaches about the eval design

This keeps benchmark history searchable without checking local-only workspace folders into git.
