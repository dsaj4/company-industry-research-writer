# Budget Audit Underfill Task

请把附带的稿件当成一份 `benchmark-driven full` 报告来审。

约束：

- primary benchmark 使用附带的 `MINIMAX` length profile
- 预算口径使用 `transferable_body_chars`
- length band 视为 `full`

你需要输出：

1. 是否通过 Gate C
2. `actual_count`
3. `budget_attainment_rate`
4. `chapter_floor_pass_rate`
5. 是否存在“前半段写满、后半段骨架化”
6. 是否应 block，而不是继续 polish

禁止因为稿子“结构看起来还行”就忽略严重 underfill。
